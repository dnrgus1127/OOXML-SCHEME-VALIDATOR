{
  "name": "validate-xml",
  "version": "0.1.0",
  "private": true,
  "description": "Developer CLI tool for validating XML files against OOXML schemas",
  "type": "module",
  "scripts": {
    "validate": "tsx src/index.ts",
    "build": "echo 'no build needed'",
    "test": "echo 'no tests'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ooxml/core": "workspace:*",
    "@ooxml/parser": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  }
}
